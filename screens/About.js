import { StackActions } from '@react-navigation/native';
import { Text, View } from 'react-native';

const About = () => {
    return(
        <View>
            <Text>About us</Text>
        </View>
    )
}